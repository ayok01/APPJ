// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Article {
  articleId Int @id @default(autoincrement())
  author   User   @relation(fields: [articleId], references: [id])
  articleType Int
  thumbnail String  
  title String?
  body String
}

model User{
  id Int @id @default(autoincrement())
  name String 
  avater String
  introduction String
  password String
  article Article[]
}